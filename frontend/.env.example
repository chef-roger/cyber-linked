# Copy to .env.local for local dev (without Docker)
# For production, set these in Vercel dashboard

# Your Render backend URL (set after deploying backend)
VITE_API_URL=https://cyberlink-backend.onrender.com
